<?php
class Config
{
	const FB_PAGE_ID 		= '233704190019951'; // Id of facebook page which feed events. May be id number (for example: 82945776796) or name alias (for example: CelebrityTheatre). 
	const EVENT_YEAR_RANGE 	= '1'; 	// Range of year to feed events
	const EVENT_YEAR_BEFORE = false; // Feed events from year before ? (true/false)
	const EVENT_YEAR_AFTER 	= true; // Feed events from year after ? (true/false)
}